import { ClaudeAccountScreen } from '@/features/settings/sections/claude-account/ClaudeAccountScreen';

// Claude account sign-in / AI credential (`/settings/claude-account`) — thin shell.
export default function SettingsClaudeAccountRoute() {
  return <ClaudeAccountScreen />;
}
