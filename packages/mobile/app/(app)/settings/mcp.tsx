import { McpServersScreen } from '@/features/settings/sections/mcp/McpServersScreen';

// MCP Servers (`/settings/mcp`) — thin shell.
export default function SettingsMcpRoute() {
  return <McpServersScreen />;
}
