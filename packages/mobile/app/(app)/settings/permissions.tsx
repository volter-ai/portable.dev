import { PermissionsScreen } from '@/features/settings/sections/permissions/PermissionsScreen';

// Device permissions (`/settings/permissions`) — thin shell.
export default function SettingsPermissionsRoute() {
  return <PermissionsScreen />;
}
