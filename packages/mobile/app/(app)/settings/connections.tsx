import { ConnectionsScreen } from '@/features/settings/sections/connections/ConnectionsScreen';

// Connected Services (`/settings/connections`) — thin shell.
export default function SettingsConnectionsRoute() {
  return <ConnectionsScreen />;
}
