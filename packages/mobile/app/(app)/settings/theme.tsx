import { ThemeSectionScreen } from '@/features/settings/sections/theme/ThemeSectionScreen';

// Theme settings (`/settings/theme`) — thin shell.
export default function SettingsThemeRoute() {
  return <ThemeSectionScreen />;
}
