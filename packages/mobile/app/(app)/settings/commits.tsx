import { CommitsScreen } from '@/features/settings/sections/commits/CommitsScreen';

// Commit attribution / AI co-author (`/settings/commits`) — thin shell.
export default function SettingsCommitsRoute() {
  return <CommitsScreen />;
}
