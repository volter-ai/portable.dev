import { SecretsScreen } from '@/features/settings/sections/secrets/SecretsScreen';

// Secrets vault (`/settings/secrets`) — thin shell.
export default function SettingsSecretsRoute() {
  return <SecretsScreen />;
}
