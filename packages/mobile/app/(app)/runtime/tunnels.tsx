import { TunnelsListScreen } from '@/features/runtime';

// `/runtime/tunnels` — active tunnels list. Thin shell → feature screen.
export default function RuntimeTunnelsRoute() {
  return <TunnelsListScreen />;
}
