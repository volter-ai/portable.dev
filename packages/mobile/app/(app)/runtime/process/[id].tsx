import { ProcessDetailScreen } from '@/features/runtime';

// `/runtime/process/:id` — process terminal detail (reads `id` from the route).
export default function RuntimeProcessDetailRoute() {
  return <ProcessDetailScreen />;
}
