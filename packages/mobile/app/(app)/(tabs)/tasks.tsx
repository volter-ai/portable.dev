import { TasksScreen } from '@/features/tasks';

// Tasks tab (`/tasks`) — the grouped GitHub issues/PRs dashboard (issue #1340,
// web `TasksPage` parity: Done Today / In Review / Todo). Thin shell.
export default function TasksTab() {
  return <TasksScreen />;
}
