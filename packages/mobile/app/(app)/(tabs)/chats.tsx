import { ChatDirectoryScreen } from '@/features/chat';

// Chats tab (`/chats`) — the paginated chat directory (US-E4-001). Thin shell.
export default function ChatsTab() {
  return <ChatDirectoryScreen />;
}
