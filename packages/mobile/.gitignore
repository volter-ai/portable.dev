# Expo / React Native build + generated artifacts
.expo/
dist/
web-build/
build/
expo-env.d.ts.bak

# Native projects (generated by `expo prebuild`)
/ios
/android

# EAS local credentials (CI-generated from GitHub secrets; never commit)
credentials.json
credentials/

# Sentry source-map upload token (CI-generated from the SENTRY_AUTH_TOKEN secret in
# release-mobile.yml; the isolated native build-phase shell reads it). Never commit.
.env.sentry-build-plugin

# Metro / Jest
.metro-health-check*
coverage/

# Misc
*.log
.DS_Store
